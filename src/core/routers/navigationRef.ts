import { createNavigationContainerRef } from "@react-navigation/native";
import { RootStackParamList } from "./root-stack-type";

export const navigationRef = createNavigationContainerRef<RootStackParamList>();
